import { Tab, Tabs } from "nextra-theme-docs";

# Get Started

The "HomenAPI" library, is available via mainstream package managers.

## Installation

Instructions for installing the library can be found below.

{/* NOTE: do NOT run a formatter for the time being; it breaks the code blocks below. */}

<Tabs items={["npm", "yarn"]}>
  <Tab>
  ```bash
  git clone https://github.com/IamHomen/HomenAPI.git
  cd HomenAPI
  npm install
  ```
  </Tab>
  <Tab>
  ```bash
  git clone https://github.com/IamHomen/HomenAPI.git
  cd HomenAPI
  yarn install
  ```
  </Tab>
</Tabs>
